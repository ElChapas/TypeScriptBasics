(() => {
  let productTitle = 'Laptop'
  let productDescription: string ="I't a new computer";
  let price: number = 200;
  const summary = `
    Title: ${productTitle}
    Description: ${productDescription}
    Pice: ${price}
  `;
})()
