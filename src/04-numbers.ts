(()=> {
  let productPrice = 100;
  productPrice = 12;

  let costumerAge: number = 30;

  let productStock: number = 100;
  if (productStock > 10) {
    console.log('entro');
  }

  let discount = parseFloat('hola');
  discount //NaN es number

  let hex = 0xf3f3f3;
  hex // Hexadecimal es number

  let bin = 0b011001010;
  bin // Binario es number

  const myNumber: Number = 10; // se tipa como la clase Number

})()
